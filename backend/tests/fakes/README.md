# Test service doubles

Deterministic Browserbase, Backboard and Firestore replacements plus an authenticated client helper. They allow repeatable tests without production credentials or outbound provider calls.

These are synthetic fixtures, not live integration evidence. See [test coverage](../README.md).
