# Development recipe data

`demo_recipe.json` is a bundled offline fixture for repeatable development. It is not a live Browserbase result. Keep demo status visible and preserve source provenance for recipes acquired from publishers.

Structured recipe validation lives in [the recipe engine](../backend/recipe_engine/README.md). User records, caches and credentials do not belong in this directory.
