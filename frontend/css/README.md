# Companion styles

`style.css` controls the desktop cooking/session interface. There is no CSS build pipeline. Verify sign-in, recipe results, session controls, narrow screens and error states after changes. [Companion overview](../README.md).
