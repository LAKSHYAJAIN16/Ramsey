# Quest evidence and notes

`kitchen-preview.png` is an Editor rendering, not an on-headset passthrough capture. Its adjacent provenance document identifies its source. It predates the pixel portrait and is not evidence of the latest assistant appearance.

For current reproduction use [the project README](../README.md); for device checks use [the build plan](../../docs/build-plan.md). No successful APK/device run is implied by an Editor screenshot.
