# Quest evidence and notes

`kitchen-preview.png` is an Editor rendering, not an on-headset passthrough capture. `legacy-client-notes.md` preserves the prior README and its historical verification record.

For current reproduction use [the project README](../README.md); for device checks use [the build plan](../../docs/build-plan.md). No successful APK/device run is implied by an Editor screenshot.
