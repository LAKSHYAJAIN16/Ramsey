# Historical package snapshot

These files predate importing the complete Unity project. The authoritative package/version files now live in `../Packages` and `../ProjectSettings`. This folder is retained as historical context, not the directory to open in Unity.

[Project setup](../README.md).
