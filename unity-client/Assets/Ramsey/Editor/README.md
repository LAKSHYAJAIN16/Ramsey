# Editor setup tools

`RamseySetup.cs` provides Ramsey configuration, scene creation and validation menu commands. Use these from the configured Unity Editor, preserving existing scenes. Static repository validation does not execute Editor scripts. [Project setup](../../../README.md).
