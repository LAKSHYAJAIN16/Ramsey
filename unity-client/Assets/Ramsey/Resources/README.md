# Runtime resources

The bundled sandwich recipe supports offline development; `ramsey-config.json` supplies backend defaults. A runtime-saved address can take precedence. Keep provider keys and pairing credentials out of bundled resources.

The offline recipe is not proof of desktop pairing or live recipe retrieval. [Project setup](../../../README.md).
