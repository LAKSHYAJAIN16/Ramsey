# Cooking scene

`RamseyKitchen.unity` is the enabled Android build scene. Its GUID must match `ProjectSettings/EditorBuildSettings.asset`. Inspect it in the configured Unity version and test passthrough/UI on Quest before making device claims. [Project setup](../../../README.md).
