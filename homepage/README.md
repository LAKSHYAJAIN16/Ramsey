# Project homepage

Served at `/`. Introduces Ramsey and links into the desktop companion at `/app/`. Start with [index.html](index.html), [styles](css/README.md), and [scripts](js/README.md).

This is presentation, not the Quest client. Product claims should match [the evidence matrix](../docs/judging.md), and future features should be labeled. Run through the backend; no independent build step is needed.
