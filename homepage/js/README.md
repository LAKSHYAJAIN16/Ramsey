# Homepage scripts

`script.js` provides page interactions; `firebase-auth.js` contains sign-in integration. CI checks JavaScript syntax. Identity flows still require configured Firebase and browser verification. [Homepage overview](../README.md).
